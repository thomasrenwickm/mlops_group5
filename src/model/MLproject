name: model
conda_env: conda.yml

entry_points:
  main:
    parameters:
      hydra_options:
        type: str
        default: ""
        description: Extra Hydra overrides (optional)
    command: >
      python run.py $(echo ${hydra_options})